# Golang Microservices Project

Учебный проект на Golang с микросервисной архитектурой, использующий gRPC для межсервисного взаимодействия и REST API Gateway.

## 🚀 Быстрый старт

### С Docker Compose (рекомендуется)

```bash
# Сборка образов
make docker-build

# Запуск всех сервисов (PostgreSQL, auth-service, rest-api)
make docker-up

# Проверка статуса
cd deploy/docker-compose && docker-compose ps

# Просмотр логов
make docker-logs
```

После запуска:
- REST API доступен на `http://localhost:8080`
- gRPC доступен на `localhost:50051`
- PostgreSQL на `localhost:5432`

### Локальный запуск

```bash
# 1. Установить зависимости
./scripts/bootstrap-dev.sh

# 2. Запустить PostgreSQL
cd deploy/docker-compose && docker-compose up -d postgres-auth

# 3. Применить миграции
make migrate-auth-up

# 4. Собрать сервисы
make build-auth
make build-rest

# 5. Запустить сервисы (в разных терминалах)
make run-auth
make run-rest
```

## 📝 Тестирование

```bash
# Healthcheck
curl http://localhost:8080/health

# Регистрация
curl -X POST http://localhost:8080/api/v1/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"SecurePass123!"}'

# Вход
curl -X POST http://localhost:8080/api/v1/auth/signin \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"SecurePass123!"}'

# Проверка токена
curl -X GET олhttp://localhost:8080/api/v1/auth/validate \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 📚 Документация

- [Архитектура проекта](docs/architecture.md) - детали схемы данных, gRPC контракты, Kafka события
- [План разработки](docs/plan.md) - технологии, сервисы, итерации
- [Декомпозиция задач](docs/task_breakdown.md) - пошаговое руководство по разработке
- [Docker Compose README](deploy/docker-compose/README.md) - подробная документация по Docker
- [Настройка VPN](docs/vpn-setup.md) - подробное руководство по работе через Amnezia VPN

## 🏗️ Структура проекта

```
.
├── api/proto/                  # Protocol Buffer определения
│   ├── auth/v1/auth.proto      # AuthService контракт
│   ├── notes/v1/notes.proto    # NotesService контракт
│   └── gen/go/                 # Сгенерированный Go код
├── services/
│   ├── auth-service/           # Аутентификация (gRPC)
│   │   ├── cmd/main.go         # Точка входа
│   │   ├── internal/           # Бизнес-логика
│   │   ├── migrations/         # SQL миграции
│   │   ├── Dockerfile          # Docker образ
│   │   └── entrypoint.sh       # Скрипт запуска
│   └── rest-api/               # REST API Gateway
│       ├── cmd/main.go         # Точка входа
│       ├── internal/
│       │   ├── handlers/       # HTTP handlers
│       │   ├── client/         # gRPC clients
│       │   └── middleware/     # HTTP middleware
│       └── Dockerfile          # Docker образ
├── pkg/                        # Общие библиотеки
│   ├── config/                 # Конфигурация
│   └── logger/                 # Логирование
├── deploy/
│   └── docker-compose/         # Docker Compose конфигурация
├── docs/                       # Документация
├── Makefile                    # Команды для разработки
└── README.md                   # Этот файл
```

## 🛠️ Команды разработки

### Docker

```bash
make docker-build      # Собрать образы
make docker-up         # Запустить сервисы
make docker-down       # Остановить сервисы
make docker-logs       # Показать логи
make docker-restart    # Перезапустить сервисы
make docker-clean      # Полная очистка
```

### Локальная разработка

```bash
make generate          # Генерация кода из proto
make build            # Сборка всех сервисов
make build-auth       # Сборка auth-service
make build-rest       # Сборка rest-api
make run-auth         # Запуск auth-service
make run-rest         # Запуск rest-api
make lint             # Линтинг
make test             # Тесты
```

### Миграции

```bash
make migrate-auth-up       # Применить миграции
make migrate-auth-down     # Откатить последнюю миграцию
make migrate-auth-status   # Статус миграций
```

## 🏛️ Архитектура

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │ HTTP/JSON :8080
       ▼
┌─────────────────────┐
│     REST API        │ (Go + Chi router)
│   - CORS            │
│   - Logging         │
│   - Error handling  │
└──────┬──────────────┘
       │ gRPC :50051
       ▼
┌─────────────────────┐      ┌──────────────┐
│   auth-service      │─────▶│ PostgreSQL   │
│   - SignUp          │      │   :5432      │
│   - SignIn          │      │              │
│   - ValidateToken   │      │ - users      │
└─────────────────────┘      │ - outbox     │
                             └──────────────┘
```

## 🔐 Реализованные паттерны

- **Микросервисная архитектура** - отдельные сервисы для аутентификации и REST API
- **gRPC** - высокопроизводительное межсервисное взаимодействие
- **API Gateway** - единая точка входа для клиентов
- **Repository Pattern** - абстракция работы с БД
- **Dependency Injection** - через интерфейсы
- **Outbox Pattern** - для надёжной публикации событий (готово к реализации)
- **12-Factor App** - конфигурация через env переменные

## 🧪 Технологии

- **Go 1.24** - язык программирования
- **gRPC** - межсервисное взаимодействие
- **Protocol Buffers** - сериализация данных
- **Chi** - HTTP роутер для REST API
- **PostgreSQL** - база данных
- **golang-migrate** - миграции БД
- **Argon2** - хеширование паролей
- **Docker & Docker Compose** - контейнеризация
- **Buf** - работа с Protocol Buffers

## 🌐 Работа через VPN (Amnezia)

Если вы запускаете проект локально в WSL и хотите предоставить доступ друзьям через VPN, следуйте этой инструкции.

### Проблема

При подключении Windows к Amnezia VPN, WSL теряет доступ к интернету из-за конфликта DNS.

### Одноразовая настройка WSL

Современное решение с использованием Mirrored Networking (WSL 2.0+):

**1. Создайте файл конфигурации WSL:**

В **PowerShell** (Windows) выполните:

```powershell
# Создайте .wslconfig с настройками для работы с VPN
$wslConfig = @"
[wsl2]
networkingMode=mirrored
dnsTunneling=true
firewall=true
autoProxy=true
"@

$wslConfig | Out-File -FilePath "$env:USERPROFILE\.wslconfig" -Encoding ASCII

# Проверьте что файл создан
Get-Content "$env:USERPROFILE\.wslconfig"
```

**2. Перезапустите WSL:**

```powershell
wsl --shutdown
```

Подождите 10 секунд и откройте WSL снова.

**3. Проверьте что интернет работает:**

В WSL:

```bash
ping -c 3 google.com
```

✅ **Готово!** Теперь WSL использует ту же сеть что и Windows, включая VPN подключение. Интернет работает одновременно и в Windows, и в WSL!

### Запуск проекта и доступ через VPN

**1. Включите Amnezia VPN на Windows**

**2. Запустите проект в WSL:**

```bash
cd /home/main/projects/golang-project
make docker-up
```

**3. Узнайте свой VPN IP адрес:**

В PowerShell (Windows):

```powershell
ipconfig
```

Найдите адаптер с названием вашего VPN соединения (обычно содержит "Amnezia", "WireGuard" или "OpenVPN"). Скопируйте IPv4 адрес, например `10.8.0.5`.

Или посмотрите в интерфейсе Amnezia клиента.

**4. Поделитесь IP с друзьями:**

Ваши друзья, подключенные к тому же VPN, смогут использовать сервис по адресу:

```
http://ВАШ_VPN_IP:8080
```

Примеры запросов для друзей:

```bash
# Healthcheck
curl http://10.8.0.5:8080/health

# Регистрация
curl -X POST http://10.8.0.5:8080/api/v1/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"SecurePass123!"}'

# Вход
curl -X POST http://10.8.0.5:8080/api/v1/auth/signin \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","password":"SecurePass123!"}'
```

### Troubleshooting

**Интернет всё ещё не работает в WSL после настройки:**

- Убедитесь что вы перезапустили WSL через `wsl --shutdown` в PowerShell
- Проверьте что файл `.wslconfig` создан: в PowerShell выполните `Get-Content "$env:USERPROFILE\.wslconfig"`
- Проверьте версию WSL: `wsl --version` (Mirrored networking требует WSL 2.0+)
- Обновите WSL: `wsl --update` (в PowerShell от администратора)
- Попробуйте пинг: `ping -c 3 google.com` в WSL

**Друзья не могут подключиться:**

- Проверьте что Docker контейнеры запущены: `cd deploy/docker-compose && docker-compose ps`
- Проверьте что порты открыты на вашей Windows машине (Windows Defender может блокировать)
- Убедитесь что друзья используют правильный VPN IP адрес (не 127.0.0.1 и не внешний IP)
- Проверьте что все находятся в одной VPN-сети

**Docker контейнеры не стартуют:**

```bash
# Посмотрите логи
make docker-logs

# Перезапустите сервисы
make docker-restart
```

Подробнее см. [docs/vpn-setup.md](docs/vpn-setup.md)

## 📋 TODO

- [ ] Реализовать JWT токены (RS256)
- [ ] Добавить notes-service
- [ ] Реализовать Saga Pattern
- [ ] Настроить Kafka для событий
- [ ] Добавить unit-тесты
- [ ] Добавить интеграционные тесты
- [ ] Kubernetes манифесты
- [ ] CI/CD pipeline

## 📄 Лицензия

MIT
